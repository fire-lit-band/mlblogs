1. seasonal plots
2. seasonal subseries plots
3. lag plots
4. ACF plot

data have trend, then autocorrelation for small lags is big and positive

When data are seasonal, the autocorrelations will be larger for the seasonal lags (at multiples of the seasonal period) than for other lags.

we usually combine the trend and cycle into a single **trend-cycle** component (often just called the **trend** for simplicity)

# adjustment

1. calendar adjustment（每个月的天数不一样）
2. population adjustment（人数变化）
3. Inflation adjustments:Data which are affected by the value of money are best adjusted before modelling.use the price index
4. Mathematical transforms

Log transfrom

power transformation

box-cox transformation

A good value of λ is one which makes the size of the seasonal variation about the same across the whole series, as that makes the forecasting model simpler. 

# decomposition

## additive

$y_t=S_t+T_t+R_t$

S:season

T:trend-cycle

R:remainder

The additive decomposition is the most appropriate if the magnitude of the seasonal fluctuations, or the variation around the trend-cycle

When the variation in the seasonal pattern, or the variation around the trend-cycle, appears to be proportional to the level of the time series, then a multiplicative decomposition is more appropriate. Multiplicative decompositions are common with economic time series.



Each grey bar represents the same length but because the plots are on different scales, the bars vary in size.

# Seasonally adjusted data

additive:$y_t-S_t$

multiplicative data $y_t/S_t$

# moving average smoothing

$$T_t=\frac{1}{m}\sum_{j=-k}^ky_{t+j}$$

m-MA

### Moving averages of moving averages

In general, a 2×m-MA is equivalent to a weighted moving average of order m+1 where all observations take the weight 1/m, except for the first and last terms which take weights 1/(2m). So, if the seasonal period is even and of order m, we use a 2×m-MA to estimate the trend-cycle. If the seasonal period is odd and of order m, we use a m-MA to estimate the trend-cycle.

A major advantage of weighted moving averages is that they yield a smoother estimate of the trend-cycle. Instead of observations entering and leaving the calculation at full weight, their weights slowly increase and then slowly decrease, resulting in a smoother curve.

## classical decomposition

### dditive decomposition

- Step 1

  If m is an even number, compute the trend-cycle component ^Tt using a 2×m-MA. If m is an odd number, compute the trend-cycle component ^Tt using an m-MA.

- Step 2

  Calculate the detrended series: yt−^Tt.

- Step 3

  detrended adjust to average 1

- Step 4

  The remainder component is calculated by subtracting the estimated seasonal and trend-cycle components: ^Rt=yt−^Tt−^St.

  1. **分组**: 将去趋势序列中属于同一“季节”的数据分为一组。例如，对于月度数据，把所有一月份的值放在一起，所有二月份的值放在一起，以此类推。
  2. **求平均**: 计算每个季节组的平均值。例如，所有一月份去趋势值的平均值，就作为一月份的“季节指数”。
  3. **调整**: 对计算出的所有季节指数进行微调，确保它们的总和等于周期 `m`（或者说，平均值为1）。这一步是为了保证季节性因素在整个周期内不会系统性地拉高或拉低趋势。
  4. **生成序列**: 将得到的这套季节指数（例如12个）不断重复，覆盖整个时间范围，就构成了完整的季节性分量 S^t*S*^*t*。

Figure [3.13](https://otexts.com/fpp3/classical-decomposition.html#fig:classical-empl) shows a classical decomposition of the total retail employment series across the US.

### drawbacks

1. 前面没数据
2. over-smooth rapid rise and fall
3. not robust to unusual value
4. unable to capture seasonal changes over time

X-11 method

SEATSmethod

# STL

STL has several advantages over classical decomposition, and the SEATS and X-11 methods:

- Unlike SEATS and X-11, STL will handle any type of seasonality, not only monthly and quarterly data.
- The seasonal component is allowed to change over time, and the rate of change can be controlled by the user.
- The smoothness of the trend-cycle can also be controlled by the user.
- It can be robust to outliers (i.e., the user can specify a robust decomposition), so that occasional unusual observations will not affect the estimates of the trend-cycle and seasonal components. They will, however, affect the remainder component.

On the other hand, STL has some disadvantages. In particular, it does not handle trading day or calendar variation automatically, and it only provides facilities for additive decompositions.

These control how rapidly the trend-cycle and seasonal components can change. Smaller values allow for more rapid changes.