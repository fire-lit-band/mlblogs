## push forward operator

$T:X\rightarrow Y$,the corresponding push-forward operator $T_\# :M(X)\rightarrow M(Y) $

实际上push forward这个算子是代表从一个测度到另一个测度转化的函数

For discrete measure,the operator只是移动了点的位置

def:$T_\#\alpha=\sum_ia_i\delta_{T(x_i)}$

For continous $\beta=T_\#\alpha\in M(Y)$for some $\alpha\in M(X)$

for testing $h\in C(Y)$

$\int_Yh(y)d\beta(y)=\int h(T(x))d\alpha(x)$

equivant,for any $B \in Y$$\beta(B)=\alpha(T^{-1}(B))$

push ofrward operator $T_\#(\alpha_1+\alpha_2)=T_\#\alpha_1+T_\#\alpha_2$

$\rho_\alpha(x)=|\det(T^{\prime}(x))|\rho_\beta(T(x)),$

T' is the Jacobian matrix of T

如果X的分布是$\alpha$

那么$T(X)$的分布是 $\beta=T_\#\alpha$

# monge problem

$\min_T\left\{\int_{\mathcal{X}}c(x,T(x))\mathrm{d}\alpha(x):T_\sharp\alpha=\beta\right\}.$

## pull back

$T^\#:C(Y)\rightarrow C(X)$define as $T^\#g=g\circ T$

$\int _Ygd(T_\#\alpha)=\int T^\#gd\alpha$

所以在ot应用在图像扭曲上要注意

# Kantorovich’

$\mathbf{U}(\mathbf{a},\mathbf{b})\overset{\mathrm{def.}}{\operatorname*{=}}\left\{\mathbf{P}\in\mathbb{R}_+^{n\times m}:\mathbf{P}\mathbb{1}_m=\mathbf{a}\quad\mathrm{and}\quad\mathbf{P}^\mathrm{T}\mathbb{1}_n=\mathbf{b}\right\},$

Kantorovich’s relaxed formulation is always symmetric

P in U(a,b) iff $P^T$ in U(b,a)

optimization problem will be 

$\mathcal{L}_{\mathbf{C}}(\mathbf{a},\mathbf{b})\overset{\mathrm{def.}}{\operatorname*{=}}\min_{\mathbf{P}\in\mathbf{U}(\mathbf{a},\mathbf{b})}\langle\mathbf{C},\mathbf{P}\rangle\overset{\mathrm{def.}}{\operatorname*{=}}\sum_{i,j}\mathbf{C}_{i,j}\mathbf{P}_{i,j}.$

(元素对元素)

可以说明kantorovich是monge的relax版本

$\mathcal{L}_{\mathbf{C}}(\mathbf{a},\mathbf{b})\leq\min_{\mathbf{P}\in\mathbf{U}(\mathbf{a},\mathbf{b})}\langle\mathbf{C},\mathbf{P}\rangle$

## kantotovich for matching

m=n,$a=b=1_n/n$

then monge的optimal和kantorovich 的optimal一样的

$\mathcal{L}_c(\alpha,\beta)\overset{\mathrm{def.}}{\operatorname*{\operatorname*{=}}}\mathcal{L}_\mathbf{C}(\mathbf{a},\mathbf{b}).$

前者意味着输入有probability weight和supporting points

## defintion

$\mathcal{U}(\alpha,\beta)\overset{\mathrm{def.}}{\operatorname*{=}}\left\{\pi\in\mathcal{M}_+^1(\mathcal{X}\times\mathcal{Y}):P_{\mathcal{X}_\sharp}\pi=\alpha\quad\mathrm{and}\quad P_{\mathcal{Y}_\sharp}\pi=\beta\right\}.$

$P_{\mathcal{X}_\sharp}$ is the push forward of the projection $P_{\mathcal{X}}$

$\pi(A\times \mathcal{Y})=\alpha(A)$,$\pi(\mathcal{X}\times A)=\beta(B)$

if $(\mathcal{X},\mathcal{Y})$ are compact spaces and c is continous, then it always have solution

probability interpretation:$\mathcal{L}_c(\alpha,\beta)=\min_{(X,Y)}\left\{\mathbb{E}_{(X,Y)}(c(X,Y)):X\sim\alpha,Y\sim\beta\right\},$

# metric properties

$C=D^p$,$D\in R^{n\times n}_+$ is a distnace on [[n]],which means

(1)D is semmetric

(2) $D_{i,j}=0$iff i=j

(3)$D_{i,k}\leq D_{i,j}+D_{j,k}$

then $W_p(a,b)=L_{D^p}(a,b)^{1/p}$ is also distance

## proof of triangle inequiality

P is optimal solution of a,b

Q is the optimal solution b,c

to avoid null cooridnates $\hat{}$

$\tilde{\mathbf{b}}_j\overset{\mathrm{def.}}{\operatorname*{=}}\mathbf{b}_j$if $b_j>0$or $\tilde{\mathbf{b}}_j=1$ otherwise

def$S=P diag(1/\tilde{\mathbf{b}}_j)Q$

$S1_n=a$

$\begin{aligned}\mathrm{W}_p(\mathbf{a},\mathbf{c})&=\left(\min_{\mathbf{P}\in U(\mathbf{a},\mathbf{c})}\langle\mathbf{P},\mathbf{D}^p\rangle\right)^{1/p}\leq\langle\mathbf{S},\mathbf{D}^p\rangle^{1/p}\\&=\left(\sum_{ik}\mathbf{D}_{ik}^p\sum_j\frac{\mathbf{P}_{ij}\mathbf{Q}_{jk}}{\tilde{\mathbf{b}}_j}\right)^{1/p}\leq\left(\sum_{ijk}\left(\mathbf{D}_{ij}+\mathbf{D}_{jk}\right)^p\frac{\mathbf{P}_{ij}\mathbf{Q}_{jk}}{\tilde{\mathbf{b}}_j}\right)^{1/p}\\&\leq\left(\sum_{ijk}\mathbf{D}_{ij}^p\frac{\mathbf{P}_{ij}\mathbf{Q}_{jk}}{\tilde{\mathbf{b}}_j}\right)^{1/p}+\left(\sum_{ijk}\mathbf{D}_{jk}^p\frac{\mathbf{P}_{ij}\mathbf{Q}_{jk}}{\tilde{\mathbf{b}}_j}\right)^{1/p}.\end{aligned}$

$\begin{aligned}\mathrm{W}_p(\mathbf{a},\mathbf{c})&\leq\left(\sum_{ij}\mathbf{D}_{ij}^p\mathbf{P}_{ij}\sum_k\frac{\mathbf{Q}_{jk}}{\tilde{\mathbf{b}}_j}\right)^{1/p}+\left(\sum_{jk}\mathbf{D}_{jk}^p\mathbf{Q}_{jk}\sum_i\frac{\mathbf{P}_{ij}}{\tilde{\mathbf{b}}_j}\right)^{1/p}\\&=\left(\sum_{ij}\mathbf{D}_{ij}^p\mathbf{P}_{ij}\right)^{1/p}+\left(\sum_{jk}\mathbf{D}_{jk}^p\mathbf{Q}_{jk}\right)^{1/p}\\&=\mathrm{W}_p(\mathbf{a},\mathbf{b})+\mathrm{W}_p(\mathbf{b},c),\end{aligned}$

不等式1：min是取最小值

不等式2 D的三角不等式

不等式三：minkowski 不等式

不等式

## intuition

the most important being that it is a weak distance, i.e. it allows one to compare singular distributions (for instance, discrete ones) whose supports do not overlap and to quantify the spatial shift between the supports of two distributions.

## weak convergence

compact domain $\mathcal{X}$,$(a_k)_k$ converge weakly to $\alpha$ iff continous funciton g,

$\int_{\mathcal{X}}g\mathrm{d}\alpha_k\to\int_{\mathcal{X}}g\mathrm{d}\alpha.$

weak convergence equivalent to $\mathcal{W}_p(\alpha_k,\alpha)\to0$

## translation

for $c(x,y)=\|x-y\|^2,T_\tau:x\rightarrow x-\tau$

$$\mathcal{W}_2(T_{\tau\sharp}\alpha,T_{\tau'\sharp}\beta)^2=\mathcal{W}_2(\alpha,\beta)^2-2\langle\tau-\tau^{\prime},\:\mathbf{m}_\alpha-\mathbf{m}_\beta\rangle+\left\|\tau-\tau^{\prime}\right\|^2,$$
where $\mathbf{m}_\alpha\overset{\mathrm{def.}}{\operatorname*{=}}\int_{\mathcal{X}}x$d$\alpha(x)\in\mathbb{R}^d$ is the mean of $\alpha.$ In particular, this implies the nice
decomposition of the distance as

$$\mathcal{W}_2(\alpha,\beta)^2=\mathcal{W}_2(\tilde{\alpha},\tilde{\beta})^2+\left\|\mathbf{m}_\alpha-\mathbf{m}_\beta\right\|^2,$$

where $(\tilde{\alpha},\tilde{\beta})$ are the “centered” zero mean measures $\tilde{\alpha}=T_{\mathbf{m}_{\alpha}\sharp}\alpha.$

$p=+\infty$

$\mathcal{W}_\infty(\alpha,\beta)\overset{\mathrm{def.}}{\operatorname*{=}}\min_{\pi\in\mathcal{U}(\alpha,\beta)}\sup_{(x,y)\in\mathrm{Supp}(\pi)}d(x,y),$

is not convet problem

$f\oplus g(x,y)\overset{\mathrm{def.}}{\operatorname*{=}}f(x)+g(y)$

$\mathbf{f}\oplus\mathbf{g}\overset{\mathrm{def.}}{\operatorname*{\operatorname*{=}}}\mathbf{f}\mathbf{1}_m^\top+\mathbf{1}_n\mathbf{g}^\top\in\mathbb{R}^{n\times m}\text{ for two vectors }\mathbf{f}\in\mathbb{R}^n,\mathbf{~g}\in\mathbb{R}^m.$

# dual problem

admit dual problem

$$\mathcal{L}_{{\mathbf{C}}}(\mathbf{a},\mathbf{b})=\max_{{(\mathbf{f},\mathbf{g})\in\mathbf{R}(\mathbf{C})}}\langle\mathbf{f},\mathbf{a}\rangle+\langle\mathbf{g},\mathbf{b}\rangle,$$

$\mathbf{R}(\mathbf{C})\overset{\mathrm{def.}}{\operatorname*{\operatorname*{=}}}\left\{(\mathbf{f},\mathbf{g})\in\mathbb{R}^n\times\mathbb{R}^m:\forall(i,j)\in[[n]]\times[[m]],\mathbf{f}\oplus\mathbf{g}\leq\mathbf{C}\right\}.$

proof

$$\min_{\mathbf{P}\geq0}\max_{(\mathbf{f},\mathbf{g})\in\mathbb{R}^n\times\mathbb{R}^m}\left\langle\mathbf{C},\mathbf{~P}\right\rangle+\langle\mathbf{a}-\mathbf{P}\mathbb{1}_m,\mathbf{~f}\rangle+\langle\mathbf{b}-\mathbf{P}^\mathrm{T}\mathbb{1}_n,\mathbf{~g}\rangle.$$

把P和f,g分开来

$$\max_{{(\mathbf{f},\mathbf{g})\in\mathbb{R}^n\times\mathbb{R}^m}}\left\langle\mathbf{a},\mathbf{~f}\right\rangle+\langle\mathbf{b},\mathbf{~g}\rangle+\min_{{\mathbf{P}\geq0}}\langle\mathbf{C}-\mathbf{f}\mathbb{1}_m{}^{\mathrm{T}}-\mathbb{1}_n\mathbf{g}^{\mathrm{T}},\mathbf{~P}\rangle.$$

the constraint be$\mathbf{C}-\mathbf{f}\mathbb{1}_m^\mathrm{T}-\mathbb{1}_n\mathbf{g}^\mathrm{T}=\mathbf{C}-\mathbf{f}\oplus\mathbf{g}\geq0.$

the support of transport plan is 

$\mathrm{Supp}(\pi)\subset\left\{(x,y)\in\mathcal{X}\times\mathcal{Y}:f(x)+g(y)=c(x,y)\right\}.$

## kantotovich potential

$\mathcal{L}_c(\alpha,\beta)=\sup_{(f,g)\in\mathcal{R}(c)}\int_\mathcal{X}f(x)\mathrm{d}\alpha(x)+\int_\mathcal{Y}g(y)\mathrm{d}\beta(y),$

$\mathcal{R}(c)\overset{\mathrm{def.}}{\operatorname*{=}}\{(f,g)\in\mathcal{C}(\mathcal{X})\times\mathcal{C}(\mathcal{Y}):\forall(x,y),f(x)+g(y)\leq c(x,y)\}.$

(f,g)is potential

the dual problem is nontraival

(1) constraint not compact

using c-transform or $d(x,y)^p$, show f,g Lipschitz

## unconstrained dual

$\mathcal{L}_{c}(\alpha,\beta)=\sup_{(f,g)\in\mathcal{C}(\mathcal{X})\times\mathcal{C}(\mathcal{Y})}\int_{\mathcal{X}}f\mathrm{d}\alpha+\int_{\mathcal{Y}}g\mathrm{d}\beta+\min_{\mathcal{X}\otimes\mathcal{Y}}(c-f\oplus g),$

$f\oplus g=f(x)+g(y)$

# Brenier theorem

$\mathcal{X}=\mathcal{Y}=\mathbb{R}^d$and$c(x,y)=\|x-y\|^2,$

exit input measure(denoted $\alpha$) has density $\rho_\alpha$

the optimal transport in Kantorovich is unique and supported on the graph (x,T(x))

$\pi=(Id,T)_{\#\alpha}$

$$\forall\:h\in\mathcal{C}(\mathcal{X}\times\mathcal{Y}),\quad\int_{\mathcal{X}\times\mathcal{Y}}h(x,y)\mathrm{d}\pi(x,y)=\int_{\mathcal{X}}h(x,T(x))\mathrm{d}\alpha(x).$$

then

(1)T is unique,$T_{\sharp \alpha}=\beta$

(2)exist convex funciton$\varphi$$T(x)=\nabla\varphi(x)$

(3)f is potential$\varphi(x)=\frac{\|x\|^2}{2}-f(x)$,

Proof:

$$\int c\mathrm{d}\pi=C_{\alpha,\beta}-2\int\langle x,\:y\rangle\mathrm{d}\pi(x,y),$$

$$C_{\alpha,\beta}=\int\left\|x\right\|^2\mathrm{d}\alpha(x)+\int\left\|y\right\|^2\mathrm{d}\beta(y)$$

so the problem turns to

$$\max_{\pi\in\mathcal{U}(\alpha,\beta)}\:\int_{\mathcal{X}\times\mathcal{Y}}\langle x,\:y\rangle\mathrm{d}\pi(x,y),$$

its dual is 

$\min_{(\varphi,\psi)}\left\{\int_{\mathcal{X}}\varphi\mathrm{d}\alpha+\int_{\mathcal{Y}}\psi\mathrm{d}\beta:\forall(x,y),\quad\varphi(x)+\psi(y)\geq\langle x,y\rangle\right\}.$

the contrainst can be replaced by$\forall\:y,\quad\psi(y)\geq\varphi^*(y)\stackrel{\mathrm{def.}}{=}\sup_x\:\langle x,\:y\rangle-\varphi(x).$

dual turns to unconstrained problem

$$\begin{aligned}\min_{\varphi}\:\int_{\mathcal{X}}\varphi\mathrm{d}\alpha+\int_{\mathcal{Y}}\varphi^*\mathrm{d}\beta;\end{aligned}$$

By iterating this argument twice, replace $\varphi$ by $\varphi^{**}$

so $\varphi$ be convex and supported in 

$$\{(x,y)\::\:\varphi(x)+\varphi^*(y)=\langle x,\:y\rangle\},$$

so $\psi(y)$ should obtain minimum, there $y\in\partial\varphi(x).$

# Monge–Ampère-type equation:

$$\det(\partial^2\varphi(x))\rho_\beta(\nabla\varphi(x))=\rho_\alpha(x)$$

$$\det(\partial^2\varphi(x))=1+\varepsilon\Delta\psi(x)+o(\varepsilon).$$ can be understood as Laplacian

the solution may not be smooth

# special case

## Kronecker cost function

$$C=\mathbb{1}_{n\times n}-\mathbb{1}_n$$

$L_c(a,b)=\|a-b\|_1$

c(x,y)=0 if x=y when $x\neq y$,then OT between $\alpha,\beta$ equal to total variation

## Empirical measure

weight is equal and points are ordered ,then $x_i<x_j,y_i<y_j$i<j

$\mathcal{W}_p(\alpha,\beta)^p=\frac{1}{n}\sum_{i=1}^n|x_i-y_i|^p,$

used for displacement interpolation

## 1D general

$\forall x\in\mathbb{R},\quad\mathcal{C}_\alpha(x)\overset{\mathrm{def.}}{\operatorname*{=}}\int_{-\infty}^x\mathrm{d}\alpha,$

$\forall r\in[0,1],\quad\mathcal{C}_\alpha^{-1}(r)=\min_x\left\{x\in\mathbb{R}\cup\{-\infty\}:\mathcal{C}_\alpha(x)\geq r\right\}.$

the generalized quantile function of $\alpha$

$\mathcal{W}_p(\alpha,\beta)^p=\left\|\mathcal{C}_\alpha^{-1}-\mathcal{C}_\beta^{-1}\right\|_{L^p([0,1])}^p=\int_0^1|\mathcal{C}_\alpha^{-1}(r)-\mathcal{C}_\beta^{-1}(r)|^p\mathrm{d}r.$

$T=\mathcal{C}_\beta^{-1}\circ\mathcal{C}_\alpha.$

## guassian

$\begin{array}{rcl}\alpha&=&\mathcal{N}(\mathbf{m}_\alpha,\mathbf{\Sigma}_\alpha)&\mathrm{and}&\beta&=\mathcal{N}(\mathbf{m}_\beta,\mathbf{\Sigma}_\beta)\end{array}$

$T:x\mapsto\mathbf{m}_{\beta}+A(x-\mathbf{m}_{\alpha}),A=\boldsymbol{\Sigma}_\alpha^{-\frac{1}{2}}\left(\boldsymbol{\Sigma}_\alpha^{\frac{1}{2}}\boldsymbol{\Sigma}_\beta\boldsymbol{\Sigma}_\alpha^{\frac{1}{2}}\right)^{\frac{1}{2}}\boldsymbol{\Sigma}_\alpha^{-\frac{1}{2}}=A^\mathrm{T},$

is the push forward map

$\begin{aligned}\rho_\beta(T(x))&=\det(2\pi\boldsymbol{\Sigma}_\beta)^{-\frac{1}{2}}\exp(-\langle T(x)-\mathbf{m}_\beta,\boldsymbol{\Sigma}_\beta^{-1}(T(x)-\mathbf{m}_\beta)\rangle)\\&=\det(2\pi\boldsymbol{\Sigma}_\beta)^{-\frac{1}{2}}\exp(-\langle x-\mathbf{m}_\alpha,A^\mathrm{T}\boldsymbol{\Sigma}_\beta^{-1}A(x-\mathbf{m}_\alpha)\rangle)\\&=\det(2\pi\boldsymbol{\Sigma}_\beta)^{-\frac{1}{2}}\exp(-\langle x-\mathbf{m}_\alpha,\boldsymbol{\Sigma}_\alpha^{-1}(x-\mathbf{m}_\alpha)\rangle),\end{aligned}$

$|\det T^{\prime}(x)|=\det A=\left(\frac{\det\boldsymbol{\Sigma}_\beta}{\det\boldsymbol{\Sigma}_\alpha}\right)^{\frac{1}{2}}$

$\mathcal{W}_2^2(\alpha,\beta)=\|\mathbf{m}_\alpha-\mathbf{m}_\beta\|^2+\mathcal{B}(\boldsymbol{\Sigma}_\alpha,\boldsymbol{\Sigma}_\beta)^2,$

$\mathcal{B}(\boldsymbol{\Sigma}_\alpha,\boldsymbol{\Sigma}_\beta)^2\overset{\mathrm{def.}}{\operatorname*{=}}\operatorname{tr}\left(\boldsymbol{\Sigma}_\alpha+\boldsymbol{\Sigma}_\beta-2(\boldsymbol{\Sigma}_\alpha^{1/2}\boldsymbol{\Sigma}_\beta\boldsymbol{\Sigma}_\alpha^{1/2})^{1/2}\right)$

## eliptic

one can first show that for two measures with given mean and covariance matrices, the distance between the two Gaussians with these respective parameters is a lower bound of the Wasserstein distance between the two measures

$\rho_\alpha(x)=\frac{1}{\sqrt{\det(\mathbf{A})}}h(\langle x-\mathbf{m}_\alpha,\mathbf{A}^{-1}(x-\mathbf{m}_\alpha)\rangle)$

$\int_{\mathbb{R}^d}h(\langle x,x\rangle)dx=1,$

then the optimal transport is the same as guassian